
/// this annotation is used to generate the registration method that register
/// every @JsonSerializable annotated model's fromJson and toJson method.
class RegisterJsonSerializable {
  const RegisterJsonSerializable();
}
